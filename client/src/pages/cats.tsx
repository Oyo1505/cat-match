import React, { FC, useContext } from "react";
import ListCats from "../components/ListCats";

const Cats: FC = () => {
  return (
    <div>
      <ListCats />
    </div>
  );
};

export default Cats;
